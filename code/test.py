class Node:
    def __init__(self, key):
        self.left = None
        self.right = None
        self.val = key

class test:
    def create_behaivor(name):
            self.root = Node(name)
            self.root.left = Node('idle')
            self.root.right = Node('move')
            self.root.right.left = Node('attack')
